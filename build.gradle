allprojects {
	group = 'pablito'
	version = '0.0.1-SNAPSHOT'
}

subprojects {
	apply plugin: 'java'

	repositories {
		mavenCentral()
	}

	dependencies {
		testCompile('junit:junit:4.12')
	}

	sourceCompatibility = 1.8
	targetCompatibility = 1.8
}
